#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>
#include <utility>
#include <array>
#include <string>

#define LOOP(i, n) for (int i = 0; i < n; ++i)
#define LOOPK(i, k, n) for (int i = k; i < n; ++i) 

#define mp(a, b) make_pair(a, b);

#define vi vector<int>
#define pb push_back

#define ll long long

using namespace std;

int main () {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int n; 
	cin >> n;

	for (int i = 0; i < n; ++i) {

	}

	return 0;
}
